#include <stdio.h>
void preencher(float **p, int x, int y);
void printP(float **p, int x, int y);

main()
{
    int i, x, y;
    float **v;

    printf("Entre com os valores de x e y \n");

    scanf("%d  %d", &x, &y);

    v = (float **)
}