#include<stdio.h>
#include<stdlib.h>

int main(){

int vet[5], x, t = 0;
    printf("Digita 5 numeros: \n");
    for(int u = 0; u < 5; u++){
        scanf("%d", &vet[x]);
        t+= vet[x];
    }

    printf("o total da soma %d", t);

return 0;
}